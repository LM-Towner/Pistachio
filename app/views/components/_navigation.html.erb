<nav class="navbar navbar-default">
    <div class="container-fluid container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <img class="navbar-brand" src="/icon.png"/>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <% if signed_in? %>
                    <a href="/home">Home</a>
                    <% end %>
                </li>
                <li>
                    <a href="/explore">Moments</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><img src="/avatar.png" class="rounded-img" height="25px" width="25px">
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <% if signed_in? %>
                        <li>
                            <a href="/user/<%=current_user.username%>">Profile</a>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li><%= link_to('Edit Profile', edit_user_registration_path) %></li>
                        <li role="separator" class="divider"></li>
                        <li><%= link_to('Log Out', destroy_user_session_path, :method => :delete) %></li>
                    <% else %>
                        <li><%= link_to('Sign Up', new_user_registration_path)%></li>
                        <li role="separator" class="divider"></li>
                        <li><%= link_to('Sign In', new_user_session_path)%></li>
                    </ul>
                    <% end %>
            </ul>
        </li>
    </ul>
</div>
</div>
</nav>
